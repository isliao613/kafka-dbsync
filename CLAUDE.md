# kafka-dbsync

Database synchronization using Debezium CDC (Change Data Capture).

## Project Structure

- `deployment/kafka-connect/docker/` - Docker-based testing environment for Debezium connectors and SMTs

## Testing Environment

See `deployment/kafka-connect/docker/README.md` for setup and testing instructions.
